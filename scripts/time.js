// will be implementing the time changing mechanics soon...

// // const mutationCallback_t = (mutationsList, observer) => {
// //     for (let mutation of mutationsList) {
// //         if (mutation.type === 'childList') {

// //         }
// //     }
// // };

// const observer_t = new MutationObserver(mutationCallback_t);
// observer_t.observe(document.body, config);